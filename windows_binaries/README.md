# Windows binaries

This folder holds the Windows binaries.
However, there is no version control on these,
hence this folder seems empty on GitHub.

The script that uploads the Windows executable to Steam assumes that
those binaries are here.
